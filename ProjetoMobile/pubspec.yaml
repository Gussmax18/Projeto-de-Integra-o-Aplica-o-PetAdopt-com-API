name: projeto_mobile
description: A new Flutter project

publish_to: 'none' # Remove essa linha se for publicar o pacote.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.0 # ou a versão mais recente

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
